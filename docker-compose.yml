services:
  postgres:
    image: postgres:15
    container_name: freire_postgres
    restart: always
    environment:
      POSTGRES_DB: freire_db
      POSTGRES_USER: freire
      POSTGRES_PASSWORD: Freire123
    ports:
      - "5436:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U freire -d freire_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: freire_redis
    restart: always
    ports:
      - "6379:6379"

  frontend:
    build:
      context: ./apps/web
    container_name: freire_frontend
    restart: always
    ports:
      - "3002:5173"
    environment:
      - DATABASE_URL=postgresql://freire:Freire123@postgres:5432/freire_db
    depends_on:
      - postgres

volumes:
  postgres_data: